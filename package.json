{
  "name": "react_portfolio",
  "version": "0.1.0",
  "homepage": "https://jenzarsen.github.io/react_portfolio",
  "private": true,
  "dependencies": {
    "cra-template": "1.2.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.0.2",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "predeploy": "npm run cleanBuild",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "GENERATE_SOURCEMAP=false react-scripts build",
    "cleanBuild": "rimraf build && npm run build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "rimraf": "^5.0.1",
    "gh-pages": "^6.2.0"
  }
}
